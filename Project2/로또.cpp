#include <iostream>
#include <algorithm>
#include <assert.h>
#include <functional>
#include <string>
#include <vector>
#include <queue>
#include <array>
#include <memory.h>

using namespace std;


int main()
{
	int K = 0;
	cin >> K;

	int Num = 0;
	
	vector<int> Lottovec;
	Lottovec.reserve(K);
	for (int i = 0; i < K; ++i)
	{
		cin >> Num;
		Lottovec.push_back(Num);
	}
	




}